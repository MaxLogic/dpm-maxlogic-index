# MaxLogic DPM Index

GitRegistry index for MaxLogic Delphi packages.

Register it with DPM:

```bat
dpm sources add -Name MaxLogic -Source https://github.com/MaxLogic/dpm-maxlogic-index.git -Type GitRegistry
```

Packages:

- `MaxLogic.Foundation` - common Delphi foundation units and runtime package.
